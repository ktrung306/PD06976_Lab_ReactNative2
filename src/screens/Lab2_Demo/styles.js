import { StyleSheet } from "react-native";

export const Style = StyleSheet.create( {
    increase: {
        alignItems: "center",
        marginTop: 200,
    },
    Start_Stop: {
        alignItems: "center",
        marginTop: 200,
    },
});